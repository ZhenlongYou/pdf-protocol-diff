# 坐标轴伪表格修复验证

用户提供的报告把旧版第292、349页的横轴文字识别为无题续表，报出删除行。目标是去除错误的表格身份，同时保留实际参数表、正文与完整原页证据。

## 来源和公开入口

权威源码为本仓库；基线 `b7b75780c1932daf2b8ad04c3147d920a1035bc4`。真实输入为 `/Users/mac/Desktop/OIF-CEI-5.1.pdf` 与 `/Users/mac/Desktop/OIF-CEI-05.3.pdf`。根据 Figure 13-14 / Table 13-8 和 Figure 16-16 定位新版296、353页，不以相近页码猜测对应关系。

原始报告为 `/Users/mac/Documents/ProtocolPdfDiffReports/protocol_diff_20260910_002918_f14cf0a18b24494b8be5084998f4b76a/protocol_diff_data.json`。其中旧292 T2 bbox为 `(246.4384,438.9600,347.2994,464.2800)`，349 T2为 `(289.2451,373.3800,380.8917,397.5000)`，均确认为曲线图横轴而非表格。根因是字体轮廓构成微小网格，旧规则依赖短文字连通及有限图题距离，无法证明大型绘图区下方标签的身份。

## 修复规则及范围

复用已有网格及文字坐标，在图题、宽大绘图区、横纵两组单调数字刻度、紧邻的单格短标签共同成立时，排除该伪表格。支持对数刻度和旋转刻度，不依赖文件名、页码、Frequency字样。严格表题和结构字段优先保护；图题按局部文字簇确认，刻度与候选之间出现正文或已有轴名时保留后方表格。过滤不删除原始页面文字或图形，不加入视觉噪声屏蔽区域。完整原页截图及轻色标记沿用上轮交付行为。

已通过独立构造发现并修正跨栏图题拼接及越过已有轴名/短正文的误删风险。单元测试覆盖缺失证据、真实小表、数值行、字段表、相邻栏、非单调刻度和既有轴名。

## 验证记录

所有原始输入SHA、页窗、耗时和报告SHA记录在 `/Users/mac/Documents/ProtocolPdfDiffReports/axis_fragments_20260910/original-public.json`。真实公开路径为 `run_diff` → `write_reports`，每个反馈页连同前后页比较，旧292仍保留Table13-8的10行，旧349无表格，两处均无轴碎片续表。

可重放检查：`.venv/bin/python docs/verification/plot_axis_probe.py docs/verification/fixtures/plot-axis/*.json`。8个手工声明场景通过PDF抽取、比较及HTML报告入口；独立oracle只核对预先声明的表格数量、保留字段及源图，不导入生产判别函数。STRICT执行计划由 `build_plot_axis_evidence.py` 绑定全部生产文件、输入、probe及oracle。基线退回会重新产生伪表；删除局部文字边界检查会误删图后小表，两项负控均必须产生指定断言失败。

最终完整测试、STRICT收据、原生界面检查及Git交付状态记录在本目录外同名证据根及项目handoff。仅为已列场景的范围验证；整本636/685页耗时和峰值内存未重测，不宣称任意PDF的识别准确率。原页窗口耗时不等价于整本性能。

旧HTML是静态报告，不会随源码更新。用户需要重启工具后重新生成报告，才能同时获得本轮识别修复和上一轮完整原页、折叠文字、1.000附录展示。
