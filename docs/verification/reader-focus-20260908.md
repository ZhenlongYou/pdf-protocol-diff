# 报告审阅工作量优化

用户要求保留新旧对应截图，同时让工具明确指出变化、减少重复找差异。验收范围是既有差异的展示与来源导航，不重新声明解析准确率，也不把未核对页当成一致。

## 验收条件

1. 字词变化明细先于对应大图；正负号、比较符、单位、条件和重复出现次数完整保留。完整原配对片段作为上下文，不凭句号/缩写猜测条件辖域。另列确切变化字词，并提供原图。
2. 定位必须从生成截图时的实际源词、物理页与实际取整裁剪范围得到；在完整章节和物理来源中均要求唯一。裁剪/屏障删除词及block之间保留断点，禁止把删除NOT后的两端拼成假原句。短定位上下文必须覆盖相应变化token；无法确定时明确降级，不按数组顺序、相似度或最近页补猜。
3. 审计中已经保存的全部正文片段在同一HTML可展开，表格超过20行也全部保留。首屏数量只是展示折叠，不能吞掉晚端数值、重复条件或需复核项。仍无完整审计的历史调用保留遗漏提示。
4. 已检测的视觉变化提供全部编号区域、旧新局部定位与完整上下文；框只作定位，不改原mask、不改变覆盖计数、也不宣称其内部都是技术变化。未知对应关系、字体映射和表格归属保持明确中性复核。
5. 已检测变化先读，需复核内容明确分类；未作自动图内比较的Figure放在补充原图区。保留全部原证据及导航可达性，不能通过隐藏未知项制造覆盖完整的假象。
6. 使用真实PDF→比较→报告入口，原生WK查看、点击不同变化、两侧同步更新、缺失侧不残留上次图像、图片不放大、窄屏无横向溢出。重新生成完整OIF报告并检查底层原始差异/来源/覆盖守恒。

## 证据与边界

状态：本轮报告阅读范围已通过实现与实际产物验证；仓库交付状态以最终 delivery-receipt 和协调门禁为准。

独立人工预期为 `/tmp/pdf-reader-focus-oracle/manual-expected.json`，涵盖正负号、条件互换、重复次数、指数单位、未知状态、错序/缺证定位、23条预算及不凭字面差异推断工程结论。开发期失败日志保留在 `work/reader-focus/`；两个独立审查的反例和实际PDF保留在外部证据目录。任务完成后在此补充最终源码、报告、测试及交互证据。

本轮可以测量首屏可读事实、定位成功/未确定数量、点击是否到正确原文以及报告事实守恒。没有用户计时实验时，不能声称人工审阅时间减少某个百分比。

## 当前验证边界

本轮完整suite最后行为变更后为1307项、158.898秒、1项条件skip，见 `work/reader-focus/full-suite-5.log`。此前四次运行及失败原因均保留；第四次已通过，但早于最后的完整数值/混合字形修复，所以不用于最终源码声明。

独立原生WebKit验证三个真实PDF对，检查双侧源图、连续切换、缺侧清空提示、宽窗与520像素窄窗，全部通过；点击后的局部框只作定位，不代表框内全部像素改变。源框转像素采用实际裁图视口交集，允许的边缘取整来自半像素几何边界，不用任意放大容差。最终交互材料归档于交付根 `reviews/webkit-interaction`。

MQ82独立真实样例的原始强要求（两段短文本均能截图定位）第一次FAIL，保留原gold和原判定。两段双侧合计330/341字符，均低于原有500字符截图门槛，输出没有源截图；这不属于“已有唯一源词却错定位”。本轮先前声明的是有源截图时可靠定位、缺失时明确降级，不扩增全部短段的截图。独立条件合同检查确认完整事实、条件/符号/单位保留，未将相同内容误报为增删，且无截图时没有猜测链接。不能声称MQ82通过双方局部导航，不能外推整个提取引擎准确性。

独立审查还发现并修复了两个本轮开发回归：绕过原有字形/公式版面读者渲染；对数字做单侧事后扩展造成看似整值增删。最终复用既有渲染，混合字形按每个片段标注不确定性，数值在文字比较前保持带符号小数/指数的原文整体，再将结果映射回严格源词坐标。完整条件不按固定词数、句号或缩写截断。

## 最终实际结果

最后行为变更后的完整回归1307项、153.750秒、1条件skip，`work/reader-focus/full-suite-final.log`。正式v2：25次实际执行、8对定向RED/GREEN、八行PASS，交付根 `test-effectiveness/final-executed-receipt.json` SHA `d258420a6acf48dde1c96c88fbc092a04a81bf8164a7b8690571cab163978111`，仅适用于本轮报告呈现/事实守恒/条件导航范围。

原生源GUI完整OIF656/685页完成1065.038秒，源始末一致；同期运行测试和打包，不用这次时间计算严格加速比。原有审计11组全部守恒；静态9017按钮、17958侧目标通过页/侧/卡片/边界检查。最终1297图全部加载、无放大或宽窄溢出，正文及像素定位实际点击正确，431条未核对覆盖记录全部保留。

第一次整本窄窗渲染发现历史目录标题长引导点撑宽页面。独立诊断证明是标题换行问题，修复只加 `overflow-wrap: anywhere`。为复用已验证的完整比较，保留原始GUI报告不动，最终报告只应用当前生成器的同一条CSS替换；代码和HTML的单处精确变换、其他所有源码及五份审计文件相同的证据在 `report-style-refresh.json`。最终报告重新经过原生宽窄/点击/图片检查；不声称在CSS改动后重新执行过整个17分钟比较。

本机新app按权威打包器生成并实际通过冻结WebKit启动，当前源始末SHA一致；保留自包含app、spec和构建证据，移除本轮重复构建目录。最终报告和app见交付根README索引。
