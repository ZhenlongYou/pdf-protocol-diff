# 内容对应修复与验收

用户要求对2026-09-13整份OIF-CEI5.1/5.3差异报告中的错配、重复增删和真实内容丢失作整体修复。权威基线为 `5f89bfb03373f20af57f8dc256961ddcb70b0cfb`；历史报告没有build commit，不能把它当作该提交生成的证明。

原报告及501条审计：`/Users/mac/Documents/ProtocolPdfDiffReports/audit_20260913_132844/`。本轮完整证据：`/Users/mac/Documents/ProtocolPdfDiffReports/repair_20260913/`。

## 实现边界

- 从实际页脚字距识别短章节污染；公式小数不再改写章节父路径。受损父路径只在唯一同叶编号、同题、完整正文一致时恢复对应；重复标题回退不能抢占更强同号正文候选。
- 相同表行即使行列归属不可靠也可建立双侧对应，但保留待核实状态；同页嵌套与紧邻续块按标题、几何及字段归组。图框内拆开的函数轴名必须有同栏图题及四条框线支持。
- 公式自动比较仍关闭，公式前的规范文字、where定义和NOTE单独保留。删除了以无序字符库存、去数字或任意字母袋证明图表归属的路径；无法证明的文字保留。逐段清理仅可使用唯一物理页上的证据，跨页或重复归属不明时不删除。
- 表格值保留MIN/MAX等标签；单侧表继承被提取成数据行的字段与单位。两层合并表头只有在真实单元格边界证明父子归属时组合。未知结构单独列入可展开清单，不等于已确认差异或一致。

## 完整报告复查与后续修复

第一次完整GUI重比基于 `c2544e0269c69908ba9a08cffe2eaa240bcea49d`，636/685页，1302.930秒。结果保存在证据根 `full-native-release/native-result.json`，源码与输入哈希在运行前后相同。独立复核文件为 `prose-release-audit.json`（293条）及 `table-release-audit.json`（126条）。该次仍有已知误报，不能当成最终修复；卡片消失不自动等于正确。

据此补充以下通用规则：

- 普通字距页脚需至少三页的同位置、同文字模板与印刷页码递进证据；出版版本页眉作为元数据。
- 明确术语/定义结构、连续散文及完整物理单元格可保留多行单元格为同一记录；数值列表或缺词框仍不能授权行对应。
- 符合实际SFNT、显式CID/GID Identity且无冲突字体程序的特殊声明组合可以检查空glyph；有轮廓的符号不删除。源字体与实际绘制验证见 `font-identity-evidence.json`。
- 完全相同提供者属性的重绘字符在比较视图去重，保留原PDF及原图；缺少advance、颜色空间、位置或标记内容等所需证据时保留。
- 脚注小号上标按唯一实际词框附回标题，并同步保留标题来源映射。数值1不因独立出现而删除。
- 公式编号不能授权吞掉标量限值、频段或后续说明；公式自动求值和二维公式对比仍关闭。旧测试要求删除SCD11限值及频段，已改为验证这些实际条件仍在且 `formula_changes=[]`。
- Setting是参数载荷；不得将不同参数因片段次序强配。无题单行续段需要双侧唯一、逐字相同且所有覆盖质量门通过。明确Figure闭合面板可阻止图元被当成表格，但不授权正文删除。

## 验证状态

`full-release.log`：1364项、1条件skip通过，只对应第一次完整报告源码。`full-followup.log`：1369项通过，只对应中间修改；`full-followup-2.log`：1373项中旧公式清理契约失败，保留失败历史。当前14项后续反例通过，两位独立审查已复验短约束/字体属性/定义数值列表反例。公开原页：定义表旧58–62/新63–67及脚注旧206–208/新210–212均已消除所查误报；短页窗缺乏整本行号证据，不用于声称整本清理完成。

最终完整套件、第二次完整GUI重比、读者报告复核、可执行STRICT v2及正式exact-OID交付尚未完成。权威逃逸缺陷账本保持open。不能把本轮单一协议对推广为任意PDF准确率；复杂图形与不可靠二维数学仍需原页复核。


## 第二次完整报告后的修正

第二次完整报告绑定`b4f8d7f78e7750ace8135bbdf2558ead5c38383e`。对应全套1378项267.542秒通过，但独立293正文及126表审计仍发现重要误报，未验收。后续原页确认C128由Figure开头的混合单元整体过滤造成，C122因公式号跨基线切分造成；修复只恢复文字边界，保留全部实际词。上下标采用唯一原行内的有序逐字符绑定，搬移已证明的小号下标，其他词不重排；负号、重复出现、缺覆盖反例仍受保护。

H27物理行的Parameter/Conditions只是单元格换行，Min/Max/Units各单一值，逐列有序字符与原词框一致后可作为一条完整记录承接。H55合并Symbol父子关系尚未证明，未用复制符号强行拆行。图2-35两版外框由不同PDF绘图方式编码，四条实际闭边可证明同一分类；纵轴候选必须有真实图像或刻度/边线及同栏图题，不能根据短文本删除。

表格公共端只用于纯插删，完整保留有/无标签单元格及位置；存在混合改动时保留全部行作为歧义与顺序证据。初稿导致15项上下文反例失败，失败日志`third-preflight.log`保留；已修正且27项overwide反例通过，最终全套待重跑。未知表的双侧截图现在直接嵌入不确定附录，JSON记录原页和bbox，不计为确认变化。


第三次GUI源a834786的1386项全套通过仍未覆盖公式后短命令和独立标量。独立反例`Avoid clipping`、`RLM >= 0.95`证明新合并可使原本保留片段被过滤；GUI已主动中止，未产出验收报告。修正将源片段保留判定设为不变量，数学合并不能降低任一源片段的保留资格；11个短命令/数值限制反例保留。后续完整结果以第四次GUI及其源码身份为准。


第四次完整运行在原C47页窗揭示单侧视觉清理后终止：只清新版让旧图示文字被错误升级为删除。`fifth-figure`复跑原页确认成对事务守卫不再制造该删除；两侧均具体成功裁剪才能共同应用，audit与display一致，直接单侧片段不因本侧证书被清理。图中阅读次序残余仍保留为待核对，未宣称已自动证明图义一致。此修正后的完整运行与最终验收仍待完成。


第五轮完整套件发现保守守卫阻碍已证明的相同正文清理，已中止GUI。第六轮允许清理后同一非空正文归并，仍拒绝C47的单侧空结果；本来已单侧的增删可以按具体物理归属清理。双侧数值变化不判等。新增截图范围大于物理图框的独立负控，检验上下文截图不能充当图元所有权。19语义案例、10负控预检通过，完整套件和整本报告验收仍待完成。


## 2026-09-14补充保护

- 相邻强标题各自保留边界；连续五行标题只在原始物理标题证据完整时合并。叶子正文对应使用实际得分，严格阈值不能被匹配理由抬高。
- 图中文字分别保留有序词框和原生字符来源。字符证书要求完整提取序列、唯一出现、原始图框、双侧唯一图题与已对应图组；同名的另一张图不能借证。单侧新增、删除以及整节缺失均不可只凭“在图内”消除。两个旧保护路径均执行图组身份约束。
- 四列合并表行保留原始四个单元格、边界、词框和两侧截图，单独导出CSV。只有图像有效、原格承接和CSV复读全部通过时，才承接正文重复文字；不据此认定参数子行对应或表格一致。TXT直接保留原字符与换行，Markdown使用能容纳单元格反引号的围栏。
- 第六次完整运行因标题边界回归中止。之后1440项全套发现1项物理行递归访问无关章节问题，现已限定递归范围并重跑全套。该失败记录保留，不代称此前完整测试通过。
- 独立反例覆盖800→900、单侧800删除、整节删除、同名跨图组借证、原词框无字符映射的正确双边清理。局部验证通过不替代下一份完整报告与501项复核。

- 报告统计改用“正文差异候选（章节）”，明确候选不代表技术含义已确认变化；“正文待核实项（未分类）”不冒充互斥分类，原因和覆盖范围继续独立保留。桌面摘要兼容读取旧报告标签。为该承诺补齐展示口径，第七次整本运行中止；不是一次完成的最终报告。

## 第九候选的有界修复与验证

- 原生上标只在完整、唯一、有序来源行及字号/基线证据支持时恢复读者排印；1020/1012的底层抽取仍保留，显示恢复10²⁰/10¹²。旧631→新642/658两次实际公开路径通过，旧页只是局部流水线基线，不当作完整章节对应证据。
- 下标修复允许完整物理行对应2–5条连续抽取行；缺字、重复、跨无关文字和几何失配均拒绝。真实1.8全文一致；27.3.1.7正文一致，附属表格原始连字符差异仍保留。数字/条件互换负控保留变化。
- 无身份续行只有原位置完整cell一致且身份骨架/长度一致时消除假增删；三处相噪表由8条降为2条，末行软换行仍待核实。普通三列Min Value/Max Value表头恢复原标签。
- 新648/652/653通过真实合并格唯一覆盖承接Note1、R_SCC11第二条件及两层Min/Max；原始cells/bounds另存JSON，非有限框和缺字段安全拒绝。新664/671/684单层表头结合完整有序源字、列边界、字体对比和维度字段；粗体800mV/Not measured真实数据均不可提升为表头。新674因下标源序未能证明而保持原格，不借字符袋消去。
- JSON补充完整body_text供复核；它是完整比较视图，不是原PDF字形流。64条预览不足项已有独立核验清单，下一整本执行后复查，不能用双侧共同缺字或卡片消失认证。
- 独立集成证据见repair_20260913/review/integrated-source-hashes.json；新增69项局部测试通过。先前1461项完整套件通过只覆盖合并格/连续行等最终增量之前，最新完整套件及整本正在执行，最终状态仍待验收。

第九首次最终套件1511项出现3fail/3error：缺格框接口兼容、多行未归属下标及物理表头到文字重建的等价序列化已修，未修改原失败期望。修正后1517项完整测试240.561秒通过（1条件skip）；20内容保全案例及10定向错误注入预检通过。即将冻结候选重比整本，尚非最终整体验收。
