# 内容对应修复与验收

用户要求对2026-09-13整份OIF-CEI5.1/5.3差异报告中的错配、重复增删和真实内容丢失作整体修复。权威基线为 `5f89bfb03373f20af57f8dc256961ddcb70b0cfb`；历史报告没有build commit，不能把它当作该提交生成的证明。

原报告及501条审计：`/Users/mac/Documents/ProtocolPdfDiffReports/audit_20260913_132844/`。本轮完整证据：`/Users/mac/Documents/ProtocolPdfDiffReports/repair_20260913/`。

## 实现边界

- 从实际页脚字距识别短章节污染；公式小数不再改写章节父路径。受损父路径只在唯一同叶编号、同题、完整正文一致时恢复对应；重复标题回退不能抢占更强同号正文候选。
- 相同表行即使行列归属不可靠也可建立双侧对应，但保留待核实状态；同页嵌套与紧邻续块按标题、几何及字段归组。图框内拆开的函数轴名必须有同栏图题及四条框线支持。
- 公式自动比较仍关闭，公式前的规范文字、where定义和NOTE单独保留。删除了以无序字符库存、去数字或任意字母袋证明图表归属的路径；无法证明的文字保留。逐段清理仅可使用唯一物理页上的证据，跨页或重复归属不明时不删除。
- 表格值保留MIN/MAX等标签；单侧表继承被提取成数据行的字段与单位。两层合并表头只有在真实单元格边界证明父子归属时组合。未知结构单独列入可展开清单，不等于已确认差异或一致。

## 验证要求与当前状态

`full-third.log`：最终当前行为下1351项，175.525秒，1条件skip，无失败。新反例涵盖受损父路径、同文不可靠表、公式旁定义与规范词、负号、跨页证据、同页三块续接、跨栏图题及上下限交换。

旧测试中五个“乱序字符足以证明删除”的预期已改为保留未证实片段；旧限值展示测试改为检查字段关系；纯改号表题测试现在明确声明可信行列及完整内容，避免把缺少质量证据的夹具误当成已验证表格。性能缓存断言保留，历史误删行为不再作为正确性oracle。

- 真实桌面入口两本完整PDF重比：进行中，结果以 `full-native/native-result.json` 为准，绑定输入及全部运行源码SHA256。
- 可执行STRICT v2：清单已准备，最终执行及缺陷账本闭环待原PDF复核。
- 两位独立exact-OID审查、分支/主线远端交付：待完成。

不能把本轮样例及单一真实协议对推广为任意PDF的语义准确率；图形语义和不可靠二维公式仍需原页复核。
