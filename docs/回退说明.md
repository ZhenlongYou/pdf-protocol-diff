# PDF 对比工具备份与回退说明

## 通用性增强前基线（最新）

为防止本轮“去 OIF 过拟合与跨文档通用性增强”不符合使用预期，开始修改前已把
上一版验收状态固定并推送为远端注释标签：

```text
backup/pdf-protocol-diff-before-generality-hardening-20260810
```

标签剥离后的源码提交为：

```text
86e1a26e6cc7c20abf7905fbddd3935ce6e41165
```

只预览该版本而不改写任何分支：

```bash
git fetch --tags origin
git switch --detach backup/pdf-protocol-diff-before-generality-hardening-20260810
python3 main.py --demo
git switch project/pdf-protocol-diff
```

若正式决定撤销本轮增强，不要使用 `git reset --hard` 或强制推送。应先确认工作区
干净，再从当前 `main` 创建可审计的 revert 提交：

```bash
git switch main
git pull --ff-only origin main
git revert --no-commit backup/pdf-protocol-diff-before-generality-hardening-20260810..main
git commit -m "revert: restore pre-generality-hardening behavior"
```

上述范围写法只适合标签之后全部提交都属于本轮增强的情况。若以后已经加入其它功能，
应只 revert 本轮交付回复记录的精确提交，不能把标签之后的所有历史一起撤销。

## 一期识别率改进前基线（更早版本）

一期识别率改进开始前，已把上一版验收提交固定并推送为远端标签：

```text
backup/pdf-protocol-diff-before-accuracy-phase1-20260810
```

该标签指向提交：

```text
67891f01dbab0aebc3e8f16cec89a495f22b028e
```

标签不会随 `main` 或 `project/pdf-protocol-diff` 分支移动。上一版已验收 HTML 仍保留在：

```text
/Users/mac/Desktop/PDF对比工具_引用出处中性验收_20260809/532/protocol_diff_20260809_145011/protocol_diff_report.html
```

### 先预览旧版，不改变当前分支

```bash
git fetch --tags origin
git switch --detach backup/pdf-protocol-diff-before-accuracy-phase1-20260810
python3 main.py --demo
```

预览结束后回到当前项目版本：

```bash
git switch project/pdf-protocol-diff
```

### 确认不满意时的正式回退

不要使用 `git reset --hard` 或强制推送。正式回退应从当前 `main` 创建新的 revert
提交，保留一期改进和回退的完整历史：

```bash
git switch main
git pull --ff-only origin main
git revert --no-commit backup/pdf-protocol-diff-before-accuracy-phase1-20260810..main
git commit -m "revert: restore pre-accuracy phase 1 behavior"
```

执行正式回退前仍应检查 `git status --short --branch`，确认没有未保存的个人修改；
然后重新运行真实 532.04/532.05 对比并推送持久项目分支和 `main`。如果标签之后还有
与本期无关的新提交，应只 revert 本期提交，不能直接 revert 整段范围。
